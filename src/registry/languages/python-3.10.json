{
  "language": "python",
  "version": "3.10",
  "extension": "py",
  "sourceFile": "main.py",
  "executionMode": "interpreted",
  "compile": null,
  "run": {
    "command": "python3",
    "args": ["{srcPath}"]
  },
  "supports": {
    "functional": true,
    "class_design": true,
    "interactive": false,
    "threads": false,
    "stdin": true,
    "stdout": true
  },
  "runtimeLibraries": ["tree", "list", "graph"],
  "docker": {
    "image": "python:3.10-slim"
  },
  "dbLanguage": "PYTHON",
  "imports": "import sys\nimport json\nfrom typing import Optional, List",
  "inputVarTemplate": "lines[{idx}].strip()",
  "parameterReadTemplate": "if len(lines) > {idx}:\n    {paramName} = {deserialize}",
  "executionCallTemplate": "import inspect, ast\n_fn = None\nif 'Solution' in globals() and isinstance(globals()['Solution'], type):\n    _solver = Solution()\n    if hasattr(_solver, '{functionName}'):\n        _fn = getattr(_solver, '{functionName}')\n    else:\n        for _name in dir(_solver):\n            if not _name.startswith('_'):\n                _m = getattr(_solver, _name)\n                if callable(_m):\n                    _fn = _m\n                    break\nif _fn is None:\n    _candidates = ['{functionName}', 'solve', 'twoSum', 'solution']\n    for _name in _candidates:\n        if _name in globals() and callable(globals()[_name]):\n            _fn = globals()[_name]\n            break\nif _fn is None:\n    _fn = next((v for v in reversed(list(globals().values())) if callable(v) and getattr(v, '__module__', '') == '__main__' and getattr(v, '__name__', '') != 'main' and not getattr(v, '__name__', '').startswith('_')), None)\nif _fn is not None:\n    _passed_args = '{paramNames}'.strip()\n    _sig_params = len(inspect.signature(_fn).parameters)\n    if _passed_args != '':\n        result = _fn({paramNames})\n    elif _sig_params > 0:\n        def _parse_raw_args(raw_text):\n            if not raw_text: return []\n            raw_clean = raw_text.strip()\n            try:\n                val = ast.literal_eval(f'({raw_clean})')\n                if isinstance(val, tuple): return list(val)\n                return [val]\n            except Exception: pass\n            args = []\n            for line in raw_clean.splitlines():\n                line = line.strip()\n                if not line: continue\n                try:\n                    val = ast.literal_eval(f'({line})')\n                    if isinstance(val, tuple): args.extend(list(val)); continue\n                    else: args.append(val); continue\n                except Exception: pass\n                try: args.append(ast.literal_eval(line)); continue\n                except Exception: pass\n                for p in line.split():\n                    p = p.strip(',')\n                    if p:\n                        try: args.append(ast.literal_eval(p))\n                        except Exception: args.append(p)\n            return args\n        _auto_args = _parse_raw_args(raw_input)\n        while len(_auto_args) < _sig_params:\n            _auto_args.append(None)\n        result = _fn(*_auto_args)\n    else:\n        result = _fn()\nelse:\n    raise NameError('Could not find solution entry point function')",
  "executionCallVoidTemplate": "import inspect, ast\n_fn = None\nif 'Solution' in globals() and isinstance(globals()['Solution'], type):\n    _solver = Solution()\n    if hasattr(_solver, '{functionName}'):\n        _fn = getattr(_solver, '{functionName}')\n    else:\n        for _name in dir(_solver):\n            if not _name.startswith('_'):\n                _m = getattr(_solver, _name)\n                if callable(_m):\n                    _fn = _m\n                    break\nif _fn is None:\n    _candidates = ['{functionName}', 'solve', 'twoSum', 'solution']\n    for _name in _candidates:\n        if _name in globals() and callable(globals()[_name]):\n            _fn = globals()[_name]\n            break\nif _fn is None:\n    _fn = next((v for v in reversed(list(globals().values())) if callable(v) and getattr(v, '__module__', '') == '__main__' and getattr(v, '__name__', '') != 'main' and not getattr(v, '__name__', '').startswith('_')), None)\nif _fn is not None:\n    _passed_args = '{paramNames}'.strip()\n    _sig_params = len(inspect.signature(_fn).parameters)\n    if _passed_args != '':\n        _fn({paramNames})\n    elif _sig_params > 0:\n        def _parse_raw_args(raw_text):\n            if not raw_text: return []\n            raw_clean = raw_text.strip()\n            try:\n                val = ast.literal_eval(f'({raw_clean})')\n                if isinstance(val, tuple): return list(val)\n                return [val]\n            except Exception: pass\n            args = []\n            for line in raw_clean.splitlines():\n                line = line.strip()\n                if not line: continue\n                try:\n                    val = ast.literal_eval(f'({line})')\n                    if isinstance(val, tuple): args.extend(list(val)); continue\n                    else: args.append(val); continue\n                except Exception: pass\n                try: args.append(ast.literal_eval(line)); continue\n                except Exception: pass\n                for p in line.split():\n                    p = p.strip(',')\n                    if p:\n                        try: args.append(ast.literal_eval(p))\n                        except Exception: args.append(p)\n            return args\n        _auto_args = _parse_raw_args(raw_input)\n        while len(_auto_args) < _sig_params:\n            _auto_args.append(None)\n        _fn(*_auto_args)\n    else:\n        _fn()\nelse:\n    raise NameError('Could not find solution entry point function')",
  "printTemplate": "print({serializeExpr})",
  "mainBodyTemplate": "def main():\n    import sys\n    raw_input = sys.stdin.read().strip()\n    if not raw_input:\n        return\n    lines = raw_input.splitlines()\n    if len(lines) < {parametersLength} and {parametersLength} == 1:\n        lines = [raw_input]\n    if len(lines) < {parametersLength}:\n        sys.stderr.write(f'Input error: expected {parametersLength} parameter lines, got {len(lines)}\\n')\n        sys.exit(1)\n    try:\n        {executionParts}\n        {printParts}\n    except Exception as e:\n      sys.stderr.write(str(e))\n      sys.exit(1)\n\nif __name__ == '__main__':\n    main()",
  "runtimeLibraryCleanupRegexes": []
}
