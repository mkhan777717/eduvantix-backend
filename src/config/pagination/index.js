'use strict';

const problem = require('./problem.config');
const contest = require('./contest.config');
const quiz = require('./quiz.config');
const user = require('./user.config');
const submission = require('./submission.config');
const institute = require('./institute.config');
const viva = require('./viva.config');

module.exports = {
  problem,
  contest,
  quiz,
  user,
  submission,
  institute,
  viva,
};
